
    <ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:VMS.TPS">

        <DataTemplate x:Key="ObjectiveTemplate" x:Name="ObjectiveTemplate" >
            <TextBlock Text="{Binding Path=Objectives.Result}" HorizontalAlignment="Left"></TextBlock>

        </DataTemplate>
        <Style x:Key="myStyle" x:Name="test" TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Height" Value="25" />
            <Setter Property="FontWeight" Value="Black" />
            <Setter Property="SeparatorBrush" Value="LightGray" />
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Border HorizontalAlignment="Stretch" Width="300"></Border>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <DataTemplate x:Key="myTemplate" x:Name="test2">
            <Border Background="SteelBlue" Width="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type DataGridTemplateColumn}},Path=ActualWidth}">
                <TextBlock>Hi there</TextBlock>
            </Border>
        </DataTemplate>
    </ResourceDictionary>
